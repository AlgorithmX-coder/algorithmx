/* Dev harness for Week 1 (Rules of Engagement) — the locked engagement template.
 * /dev/* is 404 in production (middleware). */
import Week1 from "@/app/operators/range/week01";

export default function Week1DevPage() {
  return <Week1 />;
}
